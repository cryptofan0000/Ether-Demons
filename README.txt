1. If the project directory doesn't include node_modules directory, please run the following command in the command prompt.
npm install
2. If the project directory includes node_modules directory or you run the above command, please run the following command in the command prompt.
npm run serve
3. Finally, please visite http://localhost:9011/ in web browser.